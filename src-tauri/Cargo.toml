[package]
name = "Percolist"
version = "1.0.0"
description = "Task Tool"
authors = ["you"]
license = ""
repository = ""
edition = "2021"
rust-version = "1.77.2"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[lib]
name = "app_lib"
crate-type = ["staticlib", "cdylib", "rlib"]

[build-dependencies]
tauri-build = { version = "2.2.0", features = [] }

[dependencies]
serde_json = "1"
serde = { version = "1", features = ["derive"] }
log = "0.4"
tauri = { version = "2.3", features = ["devtools"] }
tauri-plugin-log = "2.0.0-rc"
global-hotkey = "0.6"
rusqlite = { version = "0.32", features = ["bundled", "chrono"] }
chrono = { version = "0.4", features = ["serde"] }

[target.'cfg(target_os = "linux")'.dependencies]
gtk = { version = "0.18.0", features = ["v3_24"] }

[target.'cfg(target_os = "windows")'.dependencies]
windows = { version = "0.52", features = ["Win32_Foundation", "Win32_UI_WindowsAndMessaging", "Win32_Graphics_Gdi"] }

[[bin]]
name = "Percolist"
path = "src/main.rs"
# AQUI É O IMPORTANTE:
# Isso impede o terminal de abrir ao rodar o .exe
# Para apps gráficos use `windows`, para terminal use `console`
windows_subsystem = "windows"
